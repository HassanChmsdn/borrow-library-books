import { AdminInventoryModule } from "@/modules/admin-inventory";

export const metadata = {
  title: "Admin Inventory",
};

export default function AdminInventoryPage() {
  return <AdminInventoryModule />;
}
