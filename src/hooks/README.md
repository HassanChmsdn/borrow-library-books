Shared React hooks live in this directory.

- Mocked auth hooks now live in `src/lib/auth/react.tsx`, which exposes the temporary guest/member/admin helpers for client components until Auth0 replaces the mocked session layer.
